<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>
<p>
  <b>assets:</b>
  <ul>
    <% @post.weblogAssets.each do |weblogAsset|%>
      <li><%= weblogAsset.data.url%></li>
      <% end %>
    </ul>
</p>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
